@model IEnumerable<VMController.Models.ProcessosRPA>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeProcesso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StatusProcesso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VirtualMachine)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeProcesso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StatusProcesso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VirtualMachine.idVM)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdProcesso">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdProcesso">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdProcesso">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
